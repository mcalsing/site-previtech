import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import Image from "next/image";
import Link from 'next/link'
import { FontAwesomeIcon } from '@fortawesome/react-fontawesome'
import { faBrain } from '@fortawesome/free-solid-svg-icons';

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <header>
          <nav className="flex items-center justify-between flex-wrap bg-sky-500 p-2">
            <div className="flex items-center flex-shrink-0 text-white mr-6">
              <Image src="/logo3.png" alt="logo" width={200} height={24}></Image>
            </div>
            <div className="w-full block flex-grow md:flex md:items-center md:w-auto">
              <div className="text-sm md:flex-grow">
                <a href="/" className="block mt-4 md:inline-block md:mt-0 text-sky-700 hover:text-white mr-4 text-xl">
                  Home
                </a>
                <a href="/profiles" className="block mt-4 md:inline-block md:mt-0 text-sky-700 hover:text-white mr-4 text-xl">
                  Integrantes
                </a>
                <a href="/about" className="block mt-4 md:inline-block md:mt-0 text-sky-700 hover:text-white text-xl">
                  Projeto
                </a>
              </div>
            </div>
          </nav>
        </header>
      {children}</body>
    </html>
  );
}
